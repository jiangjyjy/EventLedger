Benchmark datasets are intentionally not bundled with this repository.

Download each dataset from its official source, comply with its license and access terms, and pass the local dataset path to the relevant preparation or experiment command. Do not commit private datasets, API credentials, model weights, checkpoints, or generated run artifacts.

[project]
name = "carve"
version = "0.1.0"
description = "CARVE: typed counterfactual process rewards for multi-agent LLM orchestration"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.4"]
torch = ["torch>=2.1"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
